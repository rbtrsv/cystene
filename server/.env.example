# URLs
# Local Development
SERVER_URL=http://localhost:8003
FRONTEND_URL=http://localhost:3003
# Production (Set these in Coolify environment variables)
# SERVER_URL=https://server.cystene.com
# FRONTEND_URL=https://client.cystene.com

# Database URL - Coolify PostgreSQL with SSL (public URL for local dev)
# DATABASE_URL=postgresql+asyncpg://postgres:your-password@your-server-ip:PORT/cystene?ssl=require

# Local Database URLs (commented out)
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5433/cystene_db
# TEST_DATABASE_URL=postgresql+asyncpg://username:password@localhost:5433/test_cystene_db

# Database - Coolify PostgreSQL (individual params if needed)
DB_NAME=cystene
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=your-server-ip
DB_PORT=5432

# Security & JWT Settings
SECRET_KEY=your-secret-key-generate-a-strong-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
BACKEND_CORS_ORIGINS=http://localhost:3003,https://client.cystene.com

# Stripe Settings (switch sk_test_ to sk_live_ when going live)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Google OAuth Settings
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Local Development
GOOGLE_REDIRECT_URI=http://localhost:3003/login
# Production (Set these in Coolify environment variables)
# GOOGLE_REDIRECT_URI=https://client.cystene.com/login

# Email Settings
GMAIL_USER=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password
EMAILS_ENABLED=True

# Shopify OAuth Settings
SHOPIFY_CLIENT_ID=your-shopify-client-id
SHOPIFY_CLIENT_SECRET=your-shopify-client-secret
SHOPIFY_SCOPES=read_products,read_orders
# Local Development
SHOPIFY_REDIRECT_URI=http://localhost:8003/ecommerce/shopify/callback
# Production (Set these in Coolify environment variables)
# SHOPIFY_REDIRECT_URI=https://server.cystene.com/ecommerce/shopify/callback
